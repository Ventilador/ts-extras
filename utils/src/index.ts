import { join } from 'path';
// import { createTestSerializationExamples } from './tasks/createClassExample';
const tsSharedFolder = join(process.cwd(), './ts-shared/');

export function createTestExamples() {
    // return createTestSerializationExamples(tsSharedFolder);
}
